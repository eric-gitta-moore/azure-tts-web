// ${projectRoot}/plugin.ts
import { IApi } from 'umi';

export default (api: IApi) => {
  // api.modifyHTML(($) => {
  //   $('head').append([
  //     `<script>window.routerBase = window.location.pathname;</script>`
  //   ])
  //   return $;
  // });
};
